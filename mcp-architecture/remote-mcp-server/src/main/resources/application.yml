server:
  port: 8080

spring:
  application:
    name: remote-mcp-server

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
